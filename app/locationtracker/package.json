{
  "name": "locationtracker",
  "version": "1.0.0",
  "private": true,
  "description": "CAP full stack location tracker with SAPUI5 frontend and SAP HANA-ready persistence.",
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "watch-locationtracker": "cds watch --open com.locationtracker.locationtracker/index.html?sap-ui-xx-viewCache=false --livereload false",
    "build": "ui5 build --config ui5.yaml --clean-dest --dest dist && cp xs-app.json dist/xs-app.json && (cd dist && zip -rq locationtracker.zip .)"
  },
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9",
    "@sap/ux-ui5-tooling": "^1.23.0",
    "cds-plugin-ui5": "^0.13.0",
    "@ui5/cli": "^4.0.17"
  },
  "cds": {
    "folders": {
      "db": "db",
      "srv": "srv",
      "app": "app"
    },
    "requires": {
      "db": {
        "[development]": {
          "kind": "sqlite",
          "impl": "@cap-js/sqlite",
          "credentials": {
            "url": "db/location-tracker.sqlite"
          }
        },
        "[hybrid]": {
          "kind": "hana",
          "impl": "@cap-js/hana",
          "deploy-format": "hdbtable"
        },
        "[production]": {
          "kind": "hana",
          "impl": "@cap-js/hana",
          "deploy-format": "hdbtable"
        }
      }
    }
  },
  "workspaces": [
    "app/*"
  ]
}
